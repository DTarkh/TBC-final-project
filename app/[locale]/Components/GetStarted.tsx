"use client";

const GetStarted = () => {
  const handleClicks = async () => {};

  return (
    <button className="btn btn-primary" onClick={handleClicks}>
      Get Started
    </button>
  );
};

export default GetStarted;
